image: php:5.6

services:
  - composer/composer

before_script:
- bash ci/docker_install.sh > /dev/null

stages:
  - build
  - test

job1:
  image: php:5.6
  stage: build
  script:
    - composer install
    - phpunit --configuration phpunit.xml

variables:
  GIT_SSL_NO_VERIFY: "true"